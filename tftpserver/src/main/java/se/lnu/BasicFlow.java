package se.lnu;

/**
 * Created by Jakob on 2016-03-07.
 * REMOVE THIS FILE BEFORE HANDING IN ASSIGNMENT
 */
public class BasicFlow
{
    public static void main(String[] args)
    {
        //Listen for request
        //Interpret request
        //If unvalid request return error (not 1 or 2), not


        //If read parse to read and validate read object
        //Else write object, parse and validate
        //If something wrong, handle with error response and shit
        //Else handle requests, either read or write
    }
}
